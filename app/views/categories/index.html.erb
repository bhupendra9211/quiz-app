<h1 class="text-2xl font-bold mb-4">Categories</h1>
<ul class="space-y-4">
  <% @categories.each do |category| %>
    <li class="border p-4 rounded">
      <%= link_to category.name, category_path(category), class: 'text-blue-500 font-medium hover:underline' %>
    </li>
  <% end %>
</ul>
